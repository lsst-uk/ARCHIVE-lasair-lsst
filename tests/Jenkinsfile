pipeline {
  agent { docker { image 'python:3.8' } }
  stages {
    stage('build') {
      steps {
        sh 'id'
      }
    }
    stage('test') {
      steps {
        dir('tests') {
          sh 'python test_sherlock_wrapper.py'
        }
      }
    }
  }
}
