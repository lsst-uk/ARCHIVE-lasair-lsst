pipeline {
  agent { docker { image 'gpfrancis/sherlock-test:0.2' } }
  stages {
    stage('build') {
      steps {
        sh 'id'
      }
    }
    stage('test') {
      steps {
        dir('tests') {
          sh 'python3 test_sherlock_wrapper.py'
        }
      }
    }
  }
}
