pipeline {
  agent { docker { image 'gpfrancis/sherlock-test:0.1' } }
  stages {
    stage('build') {
      steps {
        sh 'id'
      }
    }
    stage('test') {
      steps {
        dir('tests') {
          sh 'python test_sherlock_wrapper.py'
        }
      }
    }
  }
}
